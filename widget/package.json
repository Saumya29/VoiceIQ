{
  "name": "voiceiq-widget",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
